-- ~/.config/nvim/lua/user/plugin/vim-ros-ycm.lua

return {
  "kgreenek/vim-ros-ycm",
  event = "BufRead", -- Optional: Load the plugin when a file is read
  config = function()
    -- Additional configuration if needed
  end,
}
